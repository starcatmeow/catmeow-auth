events {
    use kqueue;
    worker_connections 2048;
}

http {
    server {
        location / {
            root /app;
            try_files $uri $uri/ /index.html;
        }
        location /api/ {
            proxy_pass http://backend/;
        }
    }
}